-- Ascundem anumite erori native ale Roblox-ului din consola Delta pentru a nu fi distras
local CoreGui = game:GetService("CoreGui")
local Params = {
    RepoURL = "https://raw.githubusercontent.com/luau/SynSaveInstance/main/",
    SSI = "saveinstance",
}

-- Încărcăm librăria
local synsaveinstance = loadstring(game:HttpGet(Params.RepoURL .. Params.SSI .. ".luau", true), Params.SSI)()

local Workspace = game:GetService("Workspace")
local Lighting = game:GetService("Lighting")
local Players = game:GetService("Players")

local CacheFolder = Lighting:FindFirstChild("Delta_Map_Cache_Protected")
if not CacheFolder then
    CacheFolder = Instance.new("Folder")
    CacheFolder.Name = "Delta_Map_Cache_Protected"
    CacheFolder.Parent = Lighting
end

local processedUIDs = {}
local totalObjectsCached = 0

-- Funcția de capturare a clădirilor noi în timp ce te plimbi
local function cacheInstance(inst)
    if not inst:IsA("BasePart") and not inst:IsA("Model") then return end
    if inst:IsA("Terrain") then return end -- Terenul este un obiect unic, îl tratăm la final
    if inst:IsDescendantOf(Players) then return end
    if inst:IsDescendantOf(CacheFolder) then return end
    
    local uid = inst:GetDebugId()
    
    if not processedUIDs[uid] then
        processedUIDs[uid] = true
        
        task.spawn(function()
            pcall(function()
                local clona = inst:Clone()
                if clona then
                    clona.Archivable = true
                    if clona:IsA("BasePart") then
                        clona.Anchored = true
                    else
                        for _, desc in ipairs(clona:GetDescendants()) do
                            if desc:IsA("BasePart") then
                                desc.Anchored = true
                                desc.Archivable = true
                            end
                        end
                    end
                    clona.Parent = CacheFolder
                    totalObjectsCached = totalObjectsCached + 1
                end
            end)
        end)
    end
end

print("\n\n-----------------------------------------")
print("[Delta System] V5 - SCANARE HARTĂ + TEREN PORNITĂ")
print("-----------------------------------------\n")

for _, v in ipairs(Workspace:GetDescendants()) do
    cacheInstance(v)
end

local streamListener = Workspace.DescendantAdded:Connect(cacheInstance)

local notifyRoutine = task.spawn(function()
    while true do
        task.wait(5)
        if totalObjectsCached > 0 then
            print("[Delta Tracker] Obiecte arhivate: " .. tostring(totalObjectsCached))
        end
    end
end)

-- Funcția pe care o chemi tu la final
_G.SalveazaHartaFinala = function()
    print("[Delta System] INIȚIALIZARE SALVARE JOC COMPLET... Așteaptă.")
    
    if streamListener then streamListener:Disconnect() end
    if notifyRoutine then task.cancel(notifyRoutine) end
    
    -- Aducem clădirile înapoi în vizor pentru a fi salvate
    CacheFolder.Parent = Workspace 
    
    local Options = {
        Mode = "optimized",           
        SaveTerrain = true,           -- CORECTAT: Asta va salva apa, pământul, munții etc.
        IgnoreTerrain = false,
        IsPlace = true,               -- CORECTAT: Asta forțează fișierul să fie .rbxlx (Joc complet gata de deschis)
        Decompile = false,            
        DecompileIgnore = {"CorePackages", "CoreGui"},
        FileName = "Harta_Completa_Cu_Teren",
        Object = game                 -- CORECTAT: Salvăm tot universul, nu doar workspace-ul
    }
    
    local success, err = pcall(function()
        synsaveinstance(Options)
    end)
    
    if success then
        print("\n[Delta System] ✅ EXPORT FINALIZAT CU SUCCES! Caută 'Harta_Completa_Cu_Teren' (extensia .rbxlx) în fișierele Delta.\n")
    else
        warn("\n[Delta System] ❌ EROARE: " .. tostring(err) .. "\n")
    end
    
    CacheFolder:Destroy()
    table.clear(processedUIDs)
end
