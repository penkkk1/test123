local file = io.open("game_code.txt", "w")
for _, v in pairs(game:GetDescendants()) do
    if v:IsA("Script") or v:IsA("LocalScript") then
        file:write(v.Name .. "\n" .. v.Source .. "\n\n")
    end
end
file:close()
